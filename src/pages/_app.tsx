import '../styles/global.scss'

import styles from '../pages/Test-Gallery/styles.module.scss'

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />
}

export default MyApp
